#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using pll = pair<ll, ll>;

const ll oo = {1000000000000000010LL};
const ll MAX = {200010};

template<typename T>

struct Point
{
    T x = 0, y = 0;
};


int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    Point<ll> p {1, 2}, q;
    p = q;

    return 0;
}